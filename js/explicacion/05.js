// Objeto Match

let resultado;

resultado = Math.PI //numero random
resultado = Math.random(); //numero random
resultado = Math.round(2.2) //redondea el numero
resultado = Math.ceil(2.1) // redondea hacia arriba
resultado = Math.floor(2.4) // redondea hacia abajo
resultado = Math.sqrt(144) // raiz cuadrada
resultado = Math.abs(-200) // convierte los numeros en positivos
resultado = Math.min(3, 5, 1, 8, 2, 10) // te dira el numero menor de una lista
resultado = Math.max(3, 5, 1, 8, 2, 10) // te dira el numero mayor de una lista

console.log(resultado)